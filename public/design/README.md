# Vy design assets

Drop the two approved Canva exports here using the exact names in the parent
folder's `TODO-VY.md`. The CSS already reserves their visual roles. Do not
change the dimensions or rename the files without updating `App.css`.
